#macro TILE_SIZE 32

enum ENEMYSTATE {
	
	IDLE,
	WANDER,
	FLEE,
	CHASE,
	ATTACK,
	HURT,
	DIE,
	WAIT
}

enum PLAYERSTATE {
	
	FREE,
	ATTACK_SLASH,
	ATTACK_COMBO,
	DEAD
}

enum LIST_TYPE {
	
	PAUSE,
	OPTIONS
}

enum OS {
	
	NAME,
	SEL,
	VALS
}

enum B_ACTION {
	
	PLAY,
	CREDITS,
	QUIT,
	BACK
}